machine:
  python:
    version: 2.7.12

dependencies:
  pre:
  - pip install nosetests
  - curl https://storage.googleapis.com/escape-releases-eu/escape-v0.12.3.tgz > /home/ubuntu/bin/escape.tgz
  - tar -C /home/ubuntu/bin -xvzf /home/ubuntu/bin/escape.tgz

compile:
  override:
  - escape release

test:
  override:
  - echo "Already tested"

